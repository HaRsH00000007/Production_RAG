[project]
name = "production-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Harsh Singh", email = "bhavya@headfield.com" }
]
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.15",
    "langchain-core>=1.5.5",
    "langchain-xai>=1.3.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.2",
]

[project.scripts]
production-rag = "production_rag:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
